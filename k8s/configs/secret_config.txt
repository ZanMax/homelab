Base64 config:

  {
      	"auths": {
      		"hub.docker.bar": {
      		    "auth": "base64(user:password)"
      		},
          "registry.db.rv.ua": {
      		    "auth": "base64(user:password)"
      		}
        }
    }